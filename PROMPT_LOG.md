# Лабораторная работа №14 - Разработка конвейеров обработки данных на Python и Go 
**Студент:** Фризен Марк Владимирович
**Группа:** 221331
**Вариант:** 15
# Prompt Log
**Инструмент:** deepseek v4-flash
## Задание 1: Распределённый сборщик на Go (координация через etcd).
### Промпт 1
**Промпт:** "Напиши на Go программу-сборщик отзывов с маркетплейса (имитация API Wildberries/Ozon). Программа должна подключаться к etcd, получать список product_id (шарды) через лизинг, запускать горутины для каждого product_id и собирать отзывы в структуру Review (id, product_id, rating, text, likes, dislikes, date). Покажи пример работы с клиентом etcd (clientv3) и graceful shutdown."
**Результат:** Сборка и go vet проходят без ошибок. Вот что получилось:
    Структура проекта
     1 cmd/collector/main.go   - точка входа, graceful shutdown
     2 internal/
     3 ├── models/review.go    - структура Review
     4 ├── marketplace/simulator.go   - имитация API Wildberries/Ozon
     5 ├── coordinator/coordinator.go - координация через etcd (лизинг + шардирование)
     6 └── collector/collector.go     - сборщик с горутинами

### Промпт 2
**Промпт:** "Расширь предыдущий код: добавь механизм перераспределения шардов при падении узла. Используй etcd с транзакциями и KeepAlive. Покажи, как второй экземпляр сборщика автоматически забирает шарды умершего узла. Добавь логгирование событий захвата/освобождения шардов."
**Результат:** Сборка и go vet - без ошибок.
    Что изменилось
    1. Coordinator - ключевые добавки
    2. Collector - новые механизмы
    3. Цепочка при падении узла
    4. Демонстрация в main.go
### Промпт 3
**Промпт:** "Реализуй в Go-сборщике publish собранных отзывов в NATS JetStream (тема "reviews.raw") вместо записи в файл. Обеспечь exactly-once доставку с использованием JetStream PublishAsync и обработкой подтверждений. Приведи полный пример с main(), обработкой сигналов и закрытием соединений."
**Результат:** Всё собрано и проверено. Добавлен новый модуль: internal/natspub/publisher.go
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание 2: Оконная агрегация в Go перед отправкой в Python.
### Промпт 1
**Промпт:** "Напиши на Go функцию оконной агрегации для потока отзывов. Используй tumbling window длительностью 1 минута. Для каждого product_id вычисляй: средний рейтинг, сумму лайков, количество отзывов. Результат должен быть структуры WindowAgg { ProductID, WindowStart, AvgRating, TotalLikes, ReviewCount }. Покажи реализацию на каналах с таймером сброса."
**Результат:** Собрано, go vet чисто.
### Промпт 2
**Промпт:** "Интегрируй оконную агрегацию из предыдущего пункта в сборщик отзывов (задание 1). После агрегации публикуй не сырые отзывы, а агрегированные окна в NATS (тема "reviews.windowed"). Приведи код, который атомарно сбрасывает окна каждую минуту и отправляет их в NATS, не блокируя приём новых отзывов."
**Результат:** Сборка и go vet - без ошибок.
### Промпт 3
**Промпт:** "Добавь в оконную агрегацию обработку поздних событий (watermark). Реализуй возможность «опаздывающих» отзывов – если отзыв приходит с timestamp старее текущего окна, пересчитай агрегат для того окна и отправь обновлённое значение. Используйте хеш-таблицу окон с мьютексами."
**Результат:** Всё собрано.
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~20 мин

## Задание  3: Передача данных через Apache Arrow.
### Промпт 1
**Промпт:** "Напиши Go-сервер с использованием Apache Arrow Flight RPC. Сервер должен отдавать предварительно агрегированные данные (структура WindowAgg) в формате RecordBatch. Колонки: product_id (string), window_start (timestamp), avg_rating (float64), total_likes (int64), review_count (int64). Покажи, как создать FlightServer с одним методом GetWindows()."
**Результат:** Написан Go-сервер internal/arrowserver/server.go
### Промпт 2
**Промпт:** "Напиши Python-клиент, который подключается к Go-серверу Arrow Flight, получает RecordBatch и преобразует его в Polars DataFrame без копирования. Выведи первые 5 строк и схему. Сравни время выполнения и объём переданных данных с передачей через JSON (замерь через time и sys.getsizeof)."
**Результат:** Создан файл: clients/arrow_client.py
### Промпт 3
**Промпт:** "Реализуй обратную передачу из Python в Go через Arrow Flight: Python-клиент отправляет отфильтрованный запрос (например, product_id = "100500"), Go-сервер возвращает только записи для этого товара. Используй критерии фильтрации через Flight Ticket. Приведи код на обеих сторонах."
**Результат:** Реализована обратная передача из Python в Go через Arrow Flight
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание 4: Интеграция Rust-библиотеки для валидации.
### Промпт 1
**Промпт:** "Напиши на Rust библиотеку для валидации отзыва маркетплейса. Функция validate_review(rating: f64, text: &str, likes: i32) -> Result<(), String> проверяет: rating в [1.0,5.0], text не пустой и не длиннее 5000 символов, likes >=0. Скомпилируй в CDylib и покажи, как вызвать её из Go через cgo."
**Результат:** Все 7 Rust-тестов проходят, Go-вызов через cgo работает.
### Промпт 2
**Промпт:** "Создай Python-обёртку для той же Rust-библиотеки с помощью PyO3. Функция validate_review_py(rating, text, likes) возвращает булевую переменную и сообщение об ошибке. Покажи, как собрать расширение (maturin develop) и импортировать в Python-конвейер очистки данных (Polars). Примени валидацию к каждому отзыву."
**Результат:** Создана Python-обёртка для Rust-библиотеки с помощью PyO3.
### Промпт 3
**Промпт:** "Проведи бенчмарк: сравни скорость валидации 100 000 отзывов на чистом Python (pandas apply), на Go (cgo вызов Rust) и на Python с Rust-библиотекой (PyO3). Используй модуль timeit. Предоставь график или таблицу результатов."
**Результат:** Результаты бенчмарка: валидация 100 000 отзывов
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин

## Задание 5: Развёртывание в Kubernetes с автоскалированием.
### Промпт 1
**Промпт:** "Напиши Dockerfile для Go-сборщика отзывов (сборка multi-stage). Создай Kubernetes Deployment и Service для этого сервиса. Включи readinessProbe и livenessProbe (например, проверка через отдельный эндпоинт /health). Приведи YAML-манифесты."
**Результат:** Создан Dockerfile для Go-сборщика отзывов
### Промпт 2
**Промпт:** "Добавь Horizontal Pod Autoscaler (HPA) для Go-сборщика на основе кастомной метрики – длины очереди NATS JetStream (количество необработанных сообщений в consumer). Используй Prometheus + Prometheus Adapter. Покажи конфигурацию ServiceMonitor и правило HPA."
**Результат:** 1. Код: монитор очереди NATS → Prometheus

    Новый пакет internal/monitor/queuemon.go:

     - Создаёт durable consumer review-collector-worker на стриме reviews
     - Раз в 15 сек опрашивает NATS JetStream и обновляет Prometheus-gauge:
- nats_jetstream_consumer_pending_messages{stream, consumer} -
  необработанные сообщения
- nats_jetstream_consumer_info{stream, consumer} - 1 если consumer жив
     - Метрики авторегистрируются через promauto

    Изменён cmd/collector/main.go:

     - Добавлен импорт monitor и promhttp
     - После создания NATS publisher запускается QueueMonitor в отдельной
горутине
     - На /metrics раздаётся Prometheus-эндпоинт

    2. ServiceMonitor

    deploy/k8s/servicemonitor.yaml:

      1 apiVersion: monitoring.coreos.com/v1
      2 kind: ServiceMonitor
      3 metadata:
      4   name: collector
      5   namespace: review-collector
      6 spec:
      7   selector:
      8     matchLabels:
      9app: collector
     10   endpoints:
     11     - port: http
     12path: /metrics
     13interval: 15s

    3. Prometheus Adapter ConfigMap

    deploy/k8s/prometheus-adapter-configmap.yaml:

    Правило преобразует nats_jetstream_consumer_pending_messages →
    pods/nats_queue_pending в custom.metrics.k8s.io:

      1 rules:
      2   - seriesQuery: nats_jetstream_consumer_pending_messages{...}
      3     resources:
      4overrides:
      5  namespace: { resource: "namespace" }
      6  pod:{ resource: "pod" }
      7     name:
      8as: "nats_queue_pending"
      9     metricsQuery: |
     10
 sum(nats_jetstream_consumer_pending_messages{<<.LabelMatchers>>}) by 
 (<<.GroupBy>>)

    4. HPA с кастомной метрикой

    deploy/k8s/hpa.yaml:

      1 spec:
      2   minReplicas: 2
      3   maxReplicas: 10
      4   metrics:
      5     - type: Resource
      6resource:
      7  name: cpu
      8  target:
      9    type: Utilization
     10    averageUtilization: 70
     11     - type: Resource
     12resource:
     13  name: memory
     14  target:
     15    type: Utilization
     16    averageUtilization: 80
     17     - type: Pods# <-- кастомная метрика
     18pods:
     19  metric:
     20    name: nats_queue_pending
     21  target:
     22    type: AverageValue
     23    averageValue: 500   # scale up если >500 сообщений в consumer
### Промпт 3
**Промпт:** "Настрой развёртывание всего конвейера (etcd, NATS, Go-сборщик, Python-анализатор, Streamlit) в minikube с помощью Helm-чарта. Приведи структуру чарта, values.yaml и команды для установки. Покажи, как проверить автоскалирование при увеличении нагрузки (например, скрипт генерации отзывов)."
**Результат:** Создана структура чарта
      1 deploy/helm/review-pipeline/
      2 ├── Chart.yaml   # Методанные + зависимости (etcd, nats)
      3 ├── values.yaml  # Все параметры
      4 ├── .helmignore
      5 ├── README.md
      6 ├── load-test.py # Скрипт нагрузки для теста HPA
      7 ├── analyzer/
      8 │   ├── analyzer.py     # Python NATS consumer
      9 │   ├── Dockerfile
     10 │   └── requirements.txt
     11 ├── streamlit/
     12 │   ├── dashboard.py    # Streamlit dashboard
     13 │   ├── Dockerfile
     14 │   └── requirements.txt
     15 └── templates/
     16     ├── _helpers.tpl    # Шаблонные имена/URL сервисов
     17     ├── namespace.yaml
     18     ├── collector-deployment.yaml     # Go-сборщик
     19     ├── collector-service.yaml
     20     ├── collector-hpa.yaml     # HPA (CPU + RAM + nats_queue_pending)
     21     ├── collector-servicemonitor.yaml # Prometheus ServiceMonitor
     22     ├── analyzer-deployment.yaml      # Python анализатор + PVC
     23     ├── streamlit-deployment.yaml     # Streamlit дашборд + Service
     24     └── prometheus-adapter-configmap.yaml
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

## Задание 6: Сравнение производительности: Go vs Python для сбора.
### Промпт 1
**Промпт:** "Реализуй на Python асинхронный сборщик отзывов с маркетплейса (имитация API Wildberries/Ozon) с использованием aiohttp и asyncio. Сборщик должен читать список product_id из etcd (как и Go-версия), собирать отзывы в структуру, аналогичную Go-модели Review, и публиковать их в NATS JetStream (тема "reviews.raw"). Обеспечь graceful shutdown и логирование. Приведи полный код."
**Результат:** Созданы два файла: `clients/collector.py`, `clients/requirements.txt`
### Промпт 2
**Промпт:** "Добавь в Python-сборщик сбор метрик производительности: количество собранных отзывов в секунду, потребление памяти (psutil), загрузку CPU (psutil.cpu_percent). Реализуй запись метрик в CSV-файл каждые 5 секунд. Запусти одновременно Go-сборщик (из задания 1) и Python-сборщик на одинаковой нагрузке (1000 product_id, каждый возвращает 50 отзывов). Сравни результаты и выведи таблицу сравнения (средняя скорость, пиковая память, средний CPU)."
**Результат:** На данной нагрузке Go и Python показывают практически идентичную скорость (~19100 vs ~19000 rev/s, разница <1%). Доминирующий фактор - сетевая задержка (50–200ms sleep на товар), которая нивелирует разницу в рантайме. Python потребляет ~5–10× больше памяти (RSS включает интерпретатор + объекты).
### Промпт 3
**Промпт:** "Построй графики сравнения производительности Go и Python сборщиков по трём метрикам: throughput (RPS), memory usage, CPU usage. Используй matplotlib и сохрани в PNG. Добавь подписи осей и легенду. Под графиками выведи текстовый вывод: 'Go быстрее Python в X раз по пропускной способности, потребляет в Y раз меньше памяти'."
**Результат:** Go быстрее Python в 1.01 раза по пропускной способности, Go потребляет в 1.6 раза больше памяти (RSS), Go загружает CPU в 2.5 раза меньше.Raw: Go RPS=19193, RSS=60.8MB, CPU=8.8%; Python RPS=19069, RSS=38.2MB, CPU=21.7%

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~25 мин

## Задание 7: Обработка потоковых данных (Kafka)
### Промпт 1
**Промпт:** "Замени NATS на Kafka в Go-сборщике (задание 1) и Python-анализаторе. Используй библиотеку segmentio/kafka-go для Go и aiokafka для Python. Go-сборщик публикует сырые отзывы в топик 'reviews.raw', Python-консьюмер читает и выводит статистику в реальном времени. Приведи код producer (Go) и consumer (Python) с обработкой партиций и commit сообщений."
**Результат:** Замена NATS на Kafka выполнена.
### Промпт 2
**Промпт:** "Добавь в Python-консьюмер скользящее окно (sliding window) длительностью 5 минут для подсчёта агрегатов по каждому product_id. Окно должно сдвигаться каждую минуту. Используй deque или кольцевой буфер. Вычисляй: средний рейтинг, количество отзывов, долю негативных (rating < 3). Результат выводи в консоль и публикуй в отдельный топик 'reviews.aggregated'."
**Результат:** Ключевые изменения в analyzer_kafka.py

    1. SlidingWindowAggregator - скользящее окно


    ┌───────────────┬─────────────────────────────────────────────┐
    │ Параметр      │ Значение                                    │
    ├───────────────┼─────────────────────────────────────────────┤
    │ Размер окна   │ 300 с (5 мин)                               │
    ├───────────────┼─────────────────────────────────────────────┤
    │ Сдвиг         │ 60 с (1 мин)                                │
    ├───────────────┼─────────────────────────────────────────────┤
    │ Структура     │ collections.deque[(timestamp, product_id,   │
    │ данных        │ rating), ...]                               │
    └───────────────┴─────────────────────────────────────────────┘


    Алгоритм сдвига (выполняется каждые 60 с в `_window_loop`):

     1 prune()    → удалить из deque записи с timestamp < now - 300s
     2 compute()  → сгруппировать по product_id, посчитать:
     3                avg_rating = sum(ratings) / n
     4                negative_share = count(rating < 3) / n
     5 display()  → вывод в консоль с форматированной таблицей
     6 publish()  → send + flush в Kafka топик 'reviews.aggregated'

    2. Публикация агрегатов в Kafka

     1 {
     2   "window_start":  "2026-05-28T13:22:31+00:00",
     3   "window_end":    "2026-05-28T13:27:31+00:00",
     4   "computed_at":   "2026-05-28T13:27:31+00:00",
     5   "product_id":    "WB-001",
     6   "review_count":  42,
     7   "avg_rating":    4.15,
     8   "negative_share": 0.0476
     9 }

     - key = product_id - все агрегаты одного товара в одной партиции
     - acks=1 - подтверждение от лидера
     - compression_type=snappy

    3. Интеграция

     1 msg из reviews.raw
     2   ├─► ReviewStats.record()     - кумулятивная статистика (всего, 
       RPS, топ)
     3   └─► SlidingWindow.add()      - deque-буфер (только rating 1-5)
     4        │
     5        └─ каждые 60 с:
     6             prune → compute → display → publish
       ('reviews.aggregated')

    4. Тестовый прогон

     1 Aggregates: [
     2   {"product_id": "PROD-0", "review_count": 7, "avg_rating": 2.86,
       "negative_share": 0.4286},
     3   {"product_id": "PROD-1", "review_count": 7, "avg_rating": 3.14,
       "negative_share": 0.4286},
     4   {"product_id": "PROD-2", "review_count": 6, "avg_rating": 3.0,
       "negative_share": 0.3333}
     5 ]
     6 Window stats: {'buffer_size': 20, 'total_added': 20, 'window_sec'
       : 60}

    5. Новые CLI-флаги


    ┌──────────────────┬────────────────────┬──────────────────────┐
    │ Флаг             │ По умолч.          │ Описание             │
    ├──────────────────┼────────────────────┼──────────────────────┤
    │ --agg-topic      │ reviews.aggregated │ Топик для агрегатов  │
    ├──────────────────┼────────────────────┼──────────────────────┤
    │ --window-size    │ 300                │ Размер окна (сек)    │
    ├──────────────────┼────────────────────┼──────────────────────┤
    │ --slide-interval │ 60                 │ Интервал сдвига      │
    │                  │                    │ (сек)                │
    └──────────────────┴────────────────────┴──────────────────────┘
### Промпт 3
**Промпт:** "Интегрируй скользящее окно в полный конвейер: Go-сборщик → Kafka → Python (окно + агрегация) → Kafka (результат) → Streamlit дашборд. Streamlit должен читать агрегированные данные из финального топика и обновляться в реальном времени. Покажи код дашборда с использованием `streamlit_kafka` или ручного polling. Добавь метрику задержки между моментом сбора данных и отображением на дашборде."
**Результат:** Полный конвейер интегрирован.
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~ мин

## Задание 8: Веб-дашборд с обновлением в реальном времени.
### Промпт 1
**Промпт:** "Разработай дашборд на Streamlit, который подключается к NATS и читает агрегированные окна (тема "reviews.windowed"). Используй asyncio и nats-py для асинхронного получения сообщений. Отображай текущую таблицу: product_id, avg_rating, total_likes, review_count, время последнего окна. Обновляй таблицу при каждом новом сообщении."
**Результат:** Новый дашборд переписан с нуля.
### Промпт 2
**Промпт:** "Добавь на дашборд интерактивные графики Plotly: линейный график изменения среднего рейтинга по времени для выбранного product_id, и гистограмму распределения количества отзывов по товарам. Реализуй выпадающий список для выбора товара и автообновление графиков каждые 5 секунд."
**Результат:** Добавлено/изменено:
1. Линейный график - рейтинг выбранного товара по времени
2. Гистограмма - распределение отзывов по товарам
3. Выпадающий список
4. Автообновление каждые 5 секунд
5. Структура дашборда
### Промпт 3
**Промпт:** "Расширь дашборд функцией оповещения: если средний рейтинг товара упал ниже 3.0 или количество негативных отзывов (rating < 3) превышает 30% за последние 10 минут, отправлять уведомление в Telegram бот (используйте python-telegram-bot или requests). Покажи интеграцию через отдельный поток или фоновую задачу."
**Результат:** Создан файл alerter.py и изменён dashboard.py.
### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~25 мин
