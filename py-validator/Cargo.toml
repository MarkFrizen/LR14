[package]
name = "py_review_validator"
version = "0.1.0"
edition = "2021"

[lib]
name = "py_review_validator"
crate-type = ["cdylib", "lib"]

[dependencies]
pyo3 = { version = "0.22.6", features = ["extension-module", "auto-initialize"] }

[build-dependencies]
pyo3-build-config = "0.22.6"
