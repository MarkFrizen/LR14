[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "py_review_validator"
version = "0.1.0"
description = "Rust-powered review validation via PyO3"
requires-python = ">=3.8"
