# Проверка Лабораторной работы №14 — 25.05.2026

**Репозиторий:** `https://github.com/MarkFrizen/LR14`
**Студент:** Фризен Марк Владимирович
**Группа:** 221331
**Вариант:** 15 (Повышенная сложность)
**Предметная область:** Анализ отзывов на маркетплейсах (Wildberries/Ozon)

---

## Инфраструктура

| № | Проверка | Статус | Комментарий |
|---|----------|--------|-------------|
| 1 | `git clone` | ✅ | Репозиторий на GitHub, 207 объектов |
| 2 | `go build ./...` | ✅ | Собирается без ошибок |
| 3 | `go vet ./...` | ✅ | Без замечаний |
| 4 | `go test ./...` | ✅ | **47 тестов** проходят (models=8, marketplace=10, aggregator=13, arrowserver=11, validator=5) |
| 5 | `cargo build` | ✅ | Rust-валидатор собирается |
| 6 | `cargo test` | ✅ | **7/7 passed** (граничные случаи, рейтинг, текст, лайки) |
| 7 | Python-синтаксис | ✅ | Все 10 `.py` файлов корректны |
| 8 | Размер `.git` | ✅ | **324 КБ** (после `git gc --aggressive`) |
| 9 | Бинарные артефакты в git | ✅ | `.so`, `.exe`, `target/`, `__pycache__` — игнорируются |
| 10 | `.gitignore` настроен | ✅ | Rust-артефакты, Python-кеш, бенчмарки, IDE — игнорируются |

## Документация

| № | Проверка | Статус | Комментарий |
|---|----------|--------|-------------|
| 11 | README.md: ФИО, группа, вариант, № ЛР | ✅ | Присутствуют |
| 12 | PROMPT_LOG.md заполнен | ✅ | 27 промптов по всем 6 заданиям |
| 13 | Архитектура конвейера (README) | ✅ | Mermaid-диаграмма: Go→NATS→Python→Polars→Parquet→DuckDB→Streamlit |

## Задания повышенной сложности (вариант 15)

| № | Задание | Статус | Комментарий |
|---|---------|--------|-------------|
| 1 | **Распределённый сборщик (etcd)** | ✅ | `internal/coordinator/` — лизинг, шардирование, перехват упавших узлов, транзакции KeepAlive |
| 2 | **Оконная агрегация (tumbling window)** | ✅ | `internal/aggregator/` — tumbling window, watermark, поздние события (late event recompute), `IsUpdate` для обновлений. 13 тестов |
| 3 | **Apache Arrow Flight RPC** | ✅ | `internal/arrowserver/` — Go-сервер с RecordBatch. Python-клиенты: `arrow_client.py`, `filter_client.py`. 11 тестов |
| 4 | **Rust-валидатор (cgo + PyO3)** | ✅ | 7 тестов Rust. `cgo_validator.go` — вызов через dlopen. `py-validator/` — PyO3-обёртка. `clean_reviews.py` — применение к 10K записей. 5 тестов Go |
| 5 | **Docker + K8s + HPA** | ✅ | Dockerfile (multi-stage). Helm-чарт. HPA (CPU 70% + RAM 80% + NATS queue). ServiceMonitor + Prometheus Adapter. Сравнение производительности Go vs Python |
| 6 | **Веб-дашборд (Streamlit)** | ✅ | `dashboard.py` — подключение к NATS, таблица, Plotly-графики, автообновление 5с. `alerter.py` — оповещения в Telegram при падении рейтинга < 3.0 |

## Замечания (все исправлены)

| № | Описание | Статус | Комментарий |
|---|---------|--------|-------------|
| 1 | `go test` — нет тестов | ✅ **Исправлено** | Добавлены 5 тестовых файлов: models (8), marketplace (10), aggregator (13), arrowserver (11), validator (5). Всего **47 тестов** |
| 2 | `analyzer.py` — ошибка `global` | ✅ **Исправлено** | Заменён `global FLUSH_INTERVAL` на `globals()['FLUSH_INTERVAL']` |
| 3 | Размер `.git` — 1.7M | ✅ **Исправлено** | Выполнен `git gc --aggressive` → **324 КБ** |

## Вывод

**Все замечания устранены.** Проект содержит:
- Полноценный ETL/ELT-конвейер на Go и Python
- Распределённую координацию через etcd
- Потоковую передачу через NATS JetStream
- Оконную агрегацию с обработкой поздних событий
- Apache Arrow Flight RPC для zero-copy передачи данных
- Rust-валидатор, интегрированный через cgo и PyO3
- Развёртывание в Kubernetes с автоскалированием
- Интерактивный веб-дашборд на Streamlit
- **47 модульных тестов Go + 7 тестов Rust**
